config.py
__pycache__
.idea

# Virtualenv — recreate with `uv sync` (uv.lock is committed)
.venv

# Tooling caches and coverage artifacts
.ruff_cache
.pytest_cache
.coverage
htmlcov

# Compiled message catalogs — build output. .po is the source of truth; these are
# produced by `pybabel compile` in the Docker builder and by the test fixture.
locales/**/*.mo

# Editor swap files. A .railway-config.yml.swp was swept into a commit by `git add -A`
# while the workflow was open in vim — the same way the Claude state file below was.
*.swp
*.swo

# Local Claude Code state — a per-developer permission allowlist, not a repo setting.
# One of these was swept into a feature commit by `git add -A`; ignored so it cannot be
# again. Force-add if the group ever wants a shared allowlist checked in.
.claude/
